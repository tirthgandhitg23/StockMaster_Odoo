{
  "compilerOptions": {
    /* Type Checking */
    "strict": true,                         /* Enable all strict type-checking options. */
    "skipLibCheck": true,                   /* Skip type checking of declaration files. */

    /* Modules */
    "target": "ES2020",                     /* Specify ECMAScript target version for output. */
    "module": "CommonJS",                   /* Specify module code generation (Node.js style). */
    "rootDir": "./",                        /* Specify the root directory of source files. */
    "outDir": "./dist",                     /* Specify the output directory for compiled JS. */
    "esModuleInterop": true,                /* Crucial Fix: Allows mixing ES Module imports (import) with CommonJS exports (require). */
    "forceConsistentCasingInFileNames": true, /* Ensure that casing is consistent throughout a file. */
    "moduleResolution": "node",             /* Resolve modules using Node.js style. */
    
    /* Output */
    "sourceMap": true,                      /* Generate corresponding .map files. */
    "resolveJsonModule": true,              /* Include modules imported from .json files. */
  },
  "include": [
    "**/*.ts"                               /* Include all .ts files in the current directory and subdirectories. */
  ],
  "exclude": [
    "node_modules"
  ]
}